# Leave empty to use Vite proxy in local/Docker development (recommended).
# Set to an absolute URL only when the frontend is served separately from Vite.
VITE_API_URL=

# Local fake key used by frontend storage helper.
# Replace before production deployment.
VITE_STORAGE_KEY=ambience_local_frontend_storage_key_change_before_production
